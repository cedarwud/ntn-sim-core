/**
 * Interactive UI default profile.
 *
 * The shipped first-screen default prioritizes stable SINR-driven continuity
 * with visible prepared/dual-active handover truth over the legacy Ka-band
 * donor screen. Paper-safe baseline profiles remain available in the selector.
 */

export const DEFAULT_INTERACTIVE_PROFILE_ID = 'case9-daps-baseline';
