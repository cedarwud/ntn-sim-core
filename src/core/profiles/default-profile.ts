/**
 * Interactive UI default profile.
 *
 * The shipped first-screen default now uses the dedicated truth-preserving
 * DAPS showcase profile. Benchmark-facing baselines remain available in the
 * selector and are not silently rewritten by first-screen presentation needs.
 */

export const DEFAULT_INTERACTIVE_PROFILE_ID = 'case9-daps-showcase';
