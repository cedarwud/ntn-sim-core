# MODQN Runtime (Outline)

**Status:** Outline only  
**Blocked by:** Phase 0 through Phase 5

## Future Scope

1. state builder for the reproduced baseline
2. action-space adapter for the reproduced baseline
3. reward/objective adapter for the reproduced baseline
4. training/evaluation hooks
5. baseline artifact outputs used by UI

## Note

This outline must not be treated as implementation-ready until the simulator exposes stable parameter, model, and runtime contracts.
The initial runtime implementation should stop at faithful baseline reproduction rather than variant expansion.
