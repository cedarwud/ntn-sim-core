# Fixture sweep placeholder

This fixture-only file exists so the `evaluation/sweeps/` directory surface
required by the Phase 03A producer contract is present in the sample
bundle. The consumer adapter does not descend into this directory yet;
Slice B only enforces that the directory exists.

This is a fixture stub, NOT reviewed paper evidence.
